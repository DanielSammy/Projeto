import * as React from 'react';
import { ScrollView, View } from 'react-native';
import { Footer, Header } from './GlobalStyle';


export const Home = () => {

    return(
        
        <ScrollView>
            <Header/>
                <View>

                </View>
            <Footer/>
        </ScrollView>
    )
}